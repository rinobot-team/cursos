#pragma once

class Algebra {
public:
    // Declaramos os metodos como estaticos
    static double adiciona(double num_1, double num_2);
    static double multiplica(double num_1, double num_2);
    static double potencia(double base, int expoente);
};
